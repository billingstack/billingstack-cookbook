angular.element(document).ready(function() {
        billingstack.value('config', {
                endpoint : \'<%= node['billingstack']['ui']['endpoint'] %>\'
        })
        angular.bootstrap(document, ['billingstack']);
});
